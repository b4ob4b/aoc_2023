import utils.Day
import utils.IO

fun main() {
    Day02(IO.TYPE.SAMPLE).test()
    Day02().solve()
}

class Day02(inputType: IO.TYPE = IO.TYPE.INPUT) : Day("", inputType = inputType) {
    override fun part1(): Any? {
        return "not yet implement"
    }

    override fun part2(): Any? {
        return "not yet implement"
    }
}           